// This is client side config only - don't put anything in here that shouldn't be public!
export const graphqlEndpoint = `http://${process.env.REACT_APP_KEYSTONE_HOST}/api/graphql`; // `http://keystone_backend/api/graphql`; //

export const nodejsEndpoint = `http://${process.env.REACT_APP_NODE_HOST}`; //`http://node_backend`; //

export const magentographqlEndpoint = 'http://magentodevelop24.com/graphql';