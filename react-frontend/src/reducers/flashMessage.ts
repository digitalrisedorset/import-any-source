export const flashReducer = (state: any, action: any): any => {
    // switch (action.type) {
    //     case FlashActionTypes.clearMessage:
    //         state.messages = []
    //         break;
    //     case FlashActionTypes.flashMessage:
    //         state.messages.push(action.message)
    //         //state.messageType = MessageType.success
    //         break;
    //     case FlashActionTypes.errorMessage:
    //         state.messages.push(action.message)
    //         //state.messageType = MessageType.error
    //         break;
    //     default:
    // }

    return state
}